# Default configuration file
# This file exists to prevent nginx warnings about missing default.conf
# Site-specific configurations should be placed in separate files

# Uncomment and configure if needed:
server {
    listen 80 default_server;
    server_name _;
    return 444;
}
